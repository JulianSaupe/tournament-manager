# gRPC address of the identity service (default: localhost:5000)
# Use [::1]:5000 if the server binds to IPv6 loopback
GRPC_IDENTITY_URL=[::1]:5000

# Must match AUTH_TOKEN set in the identity service
GRPC_AUTH_TOKEN=your-token-here
